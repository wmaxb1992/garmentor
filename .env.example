# All inference runs on your RunPod endpoints — no external paid APIs.

# Self-hosted chat VLM (Qwen2.5-VL-7B-Instruct on vLLM, OpenAI-compatible API).
# This is the *base URL ending in /v1* — vLLM exposes the OpenAI shape there.
# Deploy as a RunPod Pod (long-lived HTTP server). See runpod/chat-vlm/.
RUNPOD_CHAT_BASE_URL=

# Model name the API serves. Default matches the Dockerfile.
CHAT_MODEL=Qwen/Qwen2.5-VL-7B-Instruct

# GarmentGPT pattern endpoint (RunPod Serverless). See runpod/garment-gpt/.
RUNPOD_PATTERN_ENDPOINT_URL=

# 3D mesh generation endpoint (RunPod Serverless, TripoSR or TRELLIS 2).
# See runpod/RUNPOD_SETUP.md.
RUNPOD_GENERATE_ENDPOINT_URL=

# Image-to-image edit endpoint (RunPod Serverless, FLUX.1-schnell).
# See runpod/flux-edit/.
RUNPOD_EDIT_ENDPOINT_URL=

# Single RunPod API key for all of the above (Bearer auth).
# https://www.runpod.io/console/user/settings
RUNPOD_API_KEY=
