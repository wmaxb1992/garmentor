name: Build & Push RunPod Worker Images

on:
  workflow_dispatch:
    inputs:
      workers:
        description: "Comma-separated worker names (or 'all')"
        default: "all"
        required: false
  push:
    paths:
      - "runpod/**"

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}

jobs:
  build-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set.outputs.matrix }}
    steps:
      - id: set
        run: |
          # NOTE: triposr, flux-edit, and garment-gpt images now bake in model
          # weights (10-25 GB) — too large for GitHub Actions free runners.
          # Build those LOCALLY. Only cloth-sim is CI-buildable.
          INPUT="${{ github.event.inputs.workers || 'all' }}"
          if [ "$INPUT" = "all" ]; then
            echo 'matrix=["cloth-sim"]' >> "$GITHUB_OUTPUT"
          else
            ARRAY=$(echo "$INPUT" | python3 -c "import sys,json; print(json.dumps([x.strip() for x in sys.stdin.read().split(',')]))")
            echo "matrix=$ARRAY" >> "$GITHUB_OUTPUT"
          fi

  build:
    needs: build-matrix
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        worker: ${{ fromJson(needs.build-matrix.outputs.matrix) }}
        include:
          - worker: triposr
            context: runpod/triposr
            image_name: garmentor-3d
          - worker: flux-edit
            context: runpod/flux-edit
            image_name: garmentor-flux-edit
          - worker: cloth-sim
            context: runpod/cloth-sim
            image_name: garmentor-cloth-sim
          - worker: garment-gpt
            context: runpod/garment-gpt
            image_name: garmentor-pattern
    steps:
      - uses: actions/checkout@v4

      # Free disk space — the CUDA + PyTorch + model-weight images are large
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
          docker system prune -af
          df -h /

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}/${{ matrix.image_name }}:latest
            ${{ env.IMAGE_PREFIX }}/${{ matrix.image_name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
